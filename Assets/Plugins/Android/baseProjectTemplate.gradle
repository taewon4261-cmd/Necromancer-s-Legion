// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

buildscript {
    repositories {**ARTIFACTORYREPOSITORY**
        google()
        mavenCentral()
        // R8 최신 버전 저장소 (AGP 7.4.2의 D8 Kotlin 2.x StackOverflow 버그 수정)
        maven { url 'https://storage.googleapis.com/r8-releases/raw' }
    }
    dependencies {
        // firebase-auth:24.0.1 이 Kotlin 2.x 로 컴파일되어 AGP 7.4.2 D8 에서 StackOverflow 발생.
        // R8 8.x 는 Kotlin 2.x 메타데이터를 정상 처리하므로 AGP 번들 R8 를 오버라이드.
        classpath 'com.android.tools:r8:8.2.47'
        classpath 'com.android.tools.build:gradle:7.4.2'
        **BUILD_SCRIPT_DEPS**
    }
}

allprojects {
    repositories {**ARTIFACTORYREPOSITORY**
        google()
        mavenCentral()
        flatDir {
            dirs "${project(':unityLibrary').projectDir}/libs"
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
